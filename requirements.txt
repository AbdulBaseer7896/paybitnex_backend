Django
djangorestframework==3.15.2
adrf==0.1.9
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.6.0
django-filter==24.3
psycopg[binary,pool]==3.2.3
python-decouple==3.8
dj-database-url==2.3.0
Pillow==11.0.0
# S3 storage (private bucket + pre-signed URLs). django-storages
# ships an S3 backend built on boto3 that Django picks up as the
# default FileField storage via the STORAGES setting.
django-storages==1.14.4
boto3==1.35.76
# Kept transitionally so code that still references cloudinary
# doesn't crash at import time. Safe to remove once every model's
# existing files have been re-uploaded or deleted.
cloudinary==1.42.1
django-cloudinary-storage==0.3.0
celery==5.4.0
redis==5.2.1
django-redis==5.4.0
daphne==4.1.2
httpx==0.28.1
# Async SMTP client used by myapp.Utils.email_tasks for OTP and
# transactional email delivery. Replaces the previous Celery-based
# pipeline so emails go out even when no Celery worker is running.
aiosmtplib==3.0.2
python-dateutil==2.9.0
drf-spectacular==0.28.0
django-extensions==3.2.3
gunicorn==23.0.0
reportlab==4.2.5
django-anymail[resend]==12.0
